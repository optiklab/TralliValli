# TraliVali Production Environment Variables
# Copy this file to .env.prod and update with secure production values
# NEVER commit .env.prod to version control!

# ==========================================
# MongoDB Configuration
# ==========================================
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
MONGO_DATABASE=tralivali

# ==========================================
# RabbitMQ Configuration
# ==========================================
RABBITMQ_USERNAME=admin
RABBITMQ_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
RABBITMQ_VHOST=/

# ==========================================
# Redis Configuration
# ==========================================
REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# ==========================================
# JWT Configuration (REQUIRED)
# ==========================================
# Generate RSA key pair:
#   openssl genrsa -out private.pem 2048
#   openssl rsa -in private.pem -outform PEM -pubout -out public.pem
# Then copy the key contents (including BEGIN/END lines) to these variables
JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----"
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nYOUR_PUBLIC_KEY_HERE\n-----END PUBLIC KEY-----"
JWT_ISSUER=TraliVali
JWT_AUDIENCE=TraliVali
JWT_EXPIRATION_DAYS=7
JWT_REFRESH_TOKEN_EXPIRATION_DAYS=30

# ==========================================
# Azure Communication Email (Optional)
# ==========================================
AZURE_COMMUNICATION_EMAIL_CONNECTION_STRING=
EMAIL_SENDER_ADDRESS=
EMAIL_SENDER_NAME=TraliVali

# ==========================================
# Azure Blob Storage (Optional)
# ==========================================
AZURE_BLOB_STORAGE_CONNECTION_STRING=
BACKUP_CONTAINER_NAME=tralivali-backups

# ==========================================
# Invite Service
# ==========================================
# Generate a secure random key (minimum 32 characters)
INVITE_SIGNING_KEY=CHANGE_ME_SECURE_RANDOM_KEY_MIN_32_CHARS

# ==========================================
# Production Security Notes
# ==========================================
# - Change ALL default passwords above
# - Use strong, unique passwords (minimum 20 characters)
# - Store this file securely (e.g., Azure Key Vault, AWS Secrets Manager)
# - Never commit this file to version control
# - Rotate passwords regularly
# - Use different passwords for each service
# - Consider using Docker secrets for sensitive data
